import React from 'react';

import Auth from './Auth'

const App = (props) => (
    <div>
      <Auth>
        <div>
          SUPER SECRET CONTENT
        </div>
      </Auth>
    </div>
)

export default App;
